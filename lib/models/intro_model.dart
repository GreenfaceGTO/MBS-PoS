import 'package:flutter/material.dart';

class IntroModel {
  Widget gambar;
  String title;
  String subTitle;

  IntroModel(
      {required this.gambar, required this.title, required this.subTitle});
}
